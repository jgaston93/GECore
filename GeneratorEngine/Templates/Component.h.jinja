#ifndef {{ component|upper }}_HPP
#define {{ component|upper }}_HPP
{% for parameter in parameters %}
#include "DataType/{{ parameter["data_type"] }}.hpp"
{%- endfor %}

struct {{ component }}
{
  {%- for parameter in parameters %}
  {{ parameter["data_type"] }} {{ parameter["name"] }};
  {%- endfor %}
};

#endif