#ifndef COMPONENT_MANAGER_HPP
#define COMPONENT_MANAGER_HPP

{% for component in components_model %}
#include "{{ component }}.hpp"
{%- endfor %}

class ComponentManager
{

private:
  {% for component in components_model %}
  {{ component }} m_{{ component|lower }}_array;
  {%- endfor %}
};

#endif // COMPONENT_MANAGER_HPP